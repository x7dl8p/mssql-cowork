# Dependencies — never commit these; CI installs fresh
mcp-server/node_modules/

# Bundled output — generated by build step
mcp-server/bundle.js

# Plugin artifact — generated by package step
*.plugin

# Credentials — never commit
mcp-server/.env
.env

# Temp / junk files
old.env
ziIbJaxV

# OS noise
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
